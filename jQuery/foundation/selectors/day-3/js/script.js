$(document).ready(function() {
  // selecting first child of ID name called first_child
  $(".foo li:first-child").css("color", "red");

  // selecting last child of ID name called first_child
  $(".bar li:last-child").css("backgroundColor", "blue");
});
